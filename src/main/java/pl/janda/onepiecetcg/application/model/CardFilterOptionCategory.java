package pl.janda.onepiecetcg.application.model;

public enum CardFilterOptionCategory {
    TYPE,
    COLOR,
    RARITY,
    FLAT_RARITY,
    COST,
    ATTRIBUTE,
    ATTRIBUTE_COMBO,
    SUB_TYPE,
    SET,
    PREFIX,
    EFFECT
}
