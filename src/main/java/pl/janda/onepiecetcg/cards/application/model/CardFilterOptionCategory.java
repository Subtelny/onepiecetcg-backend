package pl.janda.onepiecetcg.cards.application.model;

public enum CardFilterOptionCategory {
    TYPE,
    COLOR,
    RARITY,
    FLAT_RARITY,
    COST,
    ATTRIBUTE,
    ATTRIBUTE_COMBO,
    SUB_TYPE,
    SET,
    PREFIX
}
